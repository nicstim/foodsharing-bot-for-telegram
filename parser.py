import requests
import telebot
import bs4
import sqlite3
import time

def main(bot):
    while True:
        # идет парсинг


        # рассылка "bot.send_message(user_id,post_text)"
        time.sleep(600)
